
在 Go (Gin) 中提取 `Authorization` 头部并获取 Token 字符串，主要有以下三种常用方法。

---

### 方法对比总结表

|**方法**|**实现手段**|**优点**|**缺点**|
|---|---|---|---|
|**1. 切片法**|`authHeader[7:]`|**性能最高**，无内存分配。|包含“魔法数字” 7，容易数错；需手动处理越界。|
|**2. 分割法**|`strings.SplitN()`|**最严谨**，能同时校验格式、空格和内容。|会产生临时的切片（Slice）数组，微量性能消耗。|
|**3. 修剪法**|`strings.TrimPrefix()`|**最优雅**，代码语义化最强，读起来像英语。|需要配合 `HasPrefix` 校验，否则可能提取出错误格式。|

---

### 方法详解

#### 1. 切片法 (Slicing)

利用 Go 字符串底层是字节数组的特性，直接从第 8 个字符（索引 7）开始截取。

Go

```
auth := c.GetHeader("Authorization")
if len(auth) > 7 && auth[:7] == "Bearer " {
    token := auth[7:] // 直接截取
}
```

#### 2. 分割法 (Split)

使用 `strings.SplitN` 将字符串按空格切分为两部分。

Go

```
auth := c.GetHeader("Authorization")
parts := strings.SplitN(auth, " ", 2) // 限制分割为 2 部分
if len(parts) == 2 && parts[0] == "Bearer" {
    token := parts[1]
}
```

#### 3. 修剪法 (TrimPrefix) —— **最推荐**

语义最清晰，配合前缀检查是目前工业界最主流的写法。

Go

```
auth := c.GetHeader("Authorization")
if strings.HasPrefix(auth, "Bearer ") {
    token := strings.TrimPrefix(auth, "Bearer ")
}
```

---

### 💡 核心注意事项

1. **空格陷阱**：无论用哪种方法，都要确保处理了 `Bearer` 和 `Token` 之间的那个**空格**。
    
2. **大小写敏感**：虽然 `c.GetHeader` 本身不区分大小写（你可以传 `authorization`），但 `Bearer` 后面的 Token 字符串通常是区分大小写的。
    
3. **防御式编程**：在提取之前，一定要先判断 `auth != ""`，否则在处理空字符串时可能会引起程序逻辑错误。