### 查看占用的大文件
```
du -had5 | sort -rh | head
```

- du：统计磁盘占用
    
- -h：人类可读单位（K/M/G）
    
- -a：目录 + 文件都统计
    
- -d5：最多统计到 5 层目录
    
- sort -rh：按大小 **从大到小** 排序（识别 K/M/G）
    
- head：只看 **前 10 个**
    
👉 找出当前目录下 **占空间最大的前 10 个文件/目录（深度 ≤5）**